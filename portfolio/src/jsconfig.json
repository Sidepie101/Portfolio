 /*Fixes the error message on the import statement in app.js -> The error message is a TypeScript error related to file casing. To resolve this error, I added this config 
 to make TypeScript treat all files in the 'src' directory as case-insensitive */

{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "*": ["src/*"]
      }
    }
  }

 
  